# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Phase 1 (backfill via yfinance) needs no keys.

# Phase 2+: market data / execution (paper trading first)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER=true

# Phase 2+: alerts
DISCORD_WEBHOOK_URL=

# Phase 2+: LLM sentiment (optional; FinBERT runs locally without a key)
ANTHROPIC_API_KEY=
